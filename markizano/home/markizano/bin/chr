#!/bin/php
<?php

$argv = $_SERVER['argv'];
if(!isset($argv[1]) || empty($argv[1]) || !is_numeric($argv[1])){
?>
Usage: chr [char] [-n]
char	- The character to convert
-n		- If present, output a trailing newline
<?php
	return 0;
}else{
	print chr($argv[1]);
	if(isset($arg[2]) && $arg[2] == '-n') print chr(10);
	return 1;
}

