#!/bin/bash

if [ "$1" == "-R" ] || [ "$1" == "-r" ]; then
	chmod 750 ${*};
	chown HIT:kizano -R ${*};
	echo -e "Recrusively Took ${*}";
elif [ -d $1 ] || [ -f $1 ]; then
	chmod 750 ${*};
	chown HIT:kizano ${*};
	echo -e "Took ${*}";
else
	echo -e "takeOwn: Cannot Stat ${*}. No such file or directory.";
fi

